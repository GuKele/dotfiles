alias lsl='ls -l'
alias lsa='ls -a'
alias lsla='ls -la'
alias lsal='ls -la'
alias lst='ls --tree'

alias cp='cp -i'
alias mv='mv -i'

# command it is not completely consistent with command -v, so don't alias command='command -v'
